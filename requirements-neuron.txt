# Python dependencies for AWS Trainium (Neuron) training.
#
# Differences vs requirements.txt (CUDA):
#   - REMOVED  bitsandbytes            (CUDA-only quantization)
#   - REMOVED  transformer_engine      (NVIDIA FP8, CUDA-only)
#   - REMOVED  optimum-quanto          (GPU quantization; not used on Neuron)
#   - flash-attn is never installed    (Neuron uses SDPA, see load_model._attn_impl)
#   - ADDED    optimum-neuron          (NeuronAccelerator + XLA training glue)
#
# The Neuron runtime/compiler packages (torch-neuronx, neuronx-cc, libneuronxla,
# torch_xla) are NOT listed here because they must come from the AWS Neuron pip
# index. Install them via setup_neuron.sh, then `pip install -r requirements-neuron.txt`.

safetensors
diffusers>=0.36.0
transformers==4.52.4
pyyaml
oyaml
tensorboardX
einops
accelerate
pydantic==2.11.7
omegaconf
tqdm
huggingface_hub
opencv-python
matplotlib==3.10.1
peft
tabulate
psutil
ipykernel
packaging
ninja
wheel
setuptools
sentencepiece
datasets
prodigyopt
blake3
pillow
rich
pre-commit
ruff
mypy
dotenv
types-tabulate
types-PyYAML
typing-extensions
# logging tools
wandb
swanlab
ImageHash

# --- Neuron-specific (installed separately from the AWS Neuron pip index) ---
# torch-neuronx
# neuronx-cc
# libneuronxla
optimum-neuron
